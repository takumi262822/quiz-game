= クイズゲーム: 共通処理定義書
:lang: ja
:doctype: book
:toc: left
:toclevels: 5
:toc-title: 目次
:sectnums:
:sectnumlevels: 5
:sectlinks:
:imagesdir: ./images
:icons: font
:source-highlighter: coderay
:example-caption: 例
:table-caption: 表
:figure-caption: 図

[#変更履歴, title="変更履歴", cols="1,2,3,3,3,3", options="header", align="center"]
|===
^.^|No. ^.^|版数 ^.^|変更者 <.^|作成日/変更日 <.^|変更箇所 <.^|変更内容

^.^|{counter:変更履歴} |1.00 |Takumi Harada |2026/3/31 ^.^|- |新規作成
^.^|{counter:変更履歴} |1.01 |Takumi Harada |2026/4/5 <.^|全体 <.^|ファイル名変更・書式統一（格納先・説明・変更履歴）
^.^|{counter:変更履歴} |1.02 |Takumi Harada |2026/4/5 <.^|全体 <.^|引数と戻り値を入力項目・出力項目の表に分割
|===

== 概要

本書はクイズゲーム全体で使用する共通ユーティリティの仕様を記載します。 +
DOM 反映前は必ず XSSProtection を経由し、入力値は Validator で検証してから利用する。

---

== 1. XSSProtection クラス

=== 格納先

`src/utils/xss.js`

=== 説明

DOM への文字列反映前に HTML 特殊文字をエスケープし、XSS 攻撃を防ぐ。 +
安全な URL 検証・JSON パース・クラス名設定のラッパーメソッドも提供する。

=== 静的メソッド

==== escapeHTML(str)

.入力項目
[cols="2,2,4", options="header"]
|===
^.^|引数 ^.^|型 <.^|内容

<.^|`str` |string |任意の値。`null` / 非文字列は空文字を返す。
|===

.出力項目
[cols="2,4", options="header"]
|===
^.^|型 <.^|内容

|string |`& < > " ' /` を HTML エンティティに置換した文字列
|===

.変換表
[cols="1,1", options="header"]
|===
|入力文字|変換後
|`&`|`&amp;`
|`<`|`&lt;`
|`>`|`&gt;`
|`"`|`&quot;`
|`'`|`&#39;`
|`/`|`&#x2F;`
|===

---

==== safeSetText(el, text)

.入力項目
[cols="2,2,4", options="header"]
|===
^.^|引数 ^.^|型 <.^|内容

<.^|`el` |HTMLElement |テキストをセットする対象要素
<.^|`text` |string |セットするテキスト内容
|===

.出力項目
[cols="2,4", options="header"]
|===
^.^|型 <.^|内容

|boolean |`el.textContent` に代入して XSS を防ぐ。無効な要素の場合は `false` を返す
|===

---

==== safeSetHTML(el, html, allowedTags)

.入力項目
[cols="2,2,4", options="header"]
|===
^.^|引数 ^.^|型 <.^|内容

<.^|`el` |HTMLElement |HTML をセットする対象要素
<.^|`html` |string |セットする HTML 文字列
<.^|`allowedTags` |string[] |許可するタグ名の配列。デフォルト: `b i em strong br p`
|===

.出力項目
[cols="2,4", options="header"]
|===
^.^|型 <.^|内容

|boolean |一時 div で解析し、`<script>` と `on*` ハンドラを除去してから `innerHTML` に設定する。失敗時は `false`
|===

---

==== isSafeURL(url)

.入力項目
[cols="2,2,4", options="header"]
|===
^.^|引数 ^.^|型 <.^|内容

<.^|`url` |string |検証対象の URL 文字列
|===

.出力項目
[cols="2,4", options="header"]
|===
^.^|型 <.^|内容

|boolean |`http:` `https:` `mailto:` `tel:` または相対パスのみ `true`
|===

---

==== setSafeHref(el, href)

.入力項目
[cols="2,2,4", options="header"]
|===
^.^|引数 ^.^|型 <.^|内容

<.^|`el` |HTMLElement |`href` をセットする対象要素
<.^|`href` |string |セットする URL 文字列
|===

.出力項目
[cols="2,4", options="header"]
|===
^.^|型 <.^|内容

|boolean |`isSafeURL` で検証後に `el.href` を設定する。不正な URL なら `false` を返す
|===

---

==== safeJSONParse(jsonStr)

.入力項目
[cols="2,2,4", options="header"]
|===
^.^|引数 ^.^|型 <.^|内容

<.^|`jsonStr` |string |パース対象の JSON 文字列
|===

.出力項目
[cols="2,4", options="header"]
|===
^.^|型 <.^|内容

|object\|null |`JSON.parse` をラップし、パース失敗時は `null` を返す
|===

---

==== safeSetClass(el, className)

.入力項目
[cols="2,2,4", options="header"]
|===
^.^|引数 ^.^|型 <.^|内容

<.^|`el` |HTMLElement |クラス名をセットする対象要素
<.^|`className` |string |セットするクラス名
|===

.出力項目
[cols="2,4", options="header"]
|===
^.^|型 <.^|内容

|boolean |`el.className` に安全なクラス名を設定する。失敗時は `false`
|===

---

== 2. Validator クラス

=== 格納先

`src/utils/validator.js`

=== 説明

クイズデータ・レベル値の形式チェックを行う。

=== 静的メソッド

==== isValidLevel(level)

.入力項目
[cols="2,2,4", options="header"]
|===
^.^|引数 ^.^|型 <.^|内容

<.^|`level` |string |検査対象のレベル値
|===

.出力項目
[cols="2,4", options="header"]
|===
^.^|型 <.^|内容

|boolean |`GameDefinitions.LEVELS` の値に含まれるか確認した結果
|===

---

==== isValidQuizData(data)

.入力項目
[cols="2,2,4", options="header"]
|===
^.^|引数 ^.^|型 <.^|内容

<.^|`data` |any |検査対象のクイズデータ
|===

.出力項目
[cols="2,4", options="header"]
|===
^.^|型 <.^|内容

|boolean |`Array.isArray(data) && data.length > 0` の確認結果
|===

---

==== isValidQuestion(q)

.入力項目
[cols="2,2,4", options="header"]
|===
^.^|引数 ^.^|型 <.^|内容

<.^|`q` |object |検査対象の問題オブジェクト
|===

.出力項目
[cols="2,4", options="header"]
|===
^.^|型 <.^|内容

|boolean |`question`（string）・`choices`（Array, 2件以上）・`answer`（number, 範囲内）の複合チェック結果
|===

---

== 3. Header クラス

=== 格納先

`src/ui/header.js`

=== 説明

画面上部のヘッダー要素を生成し DOM へ追加する。

=== メソッド

==== constructor(root, title)

.入力項目
[cols="2,2,4", options="header"]
|===
^.^|引数 ^.^|型 <.^|内容

<.^|`root` |Element |ヘッダーを挿入する親要素
<.^|`title` |string |ヘッダーに表示するタイトル
|===

.出力項目
なし

NOTE: ヘッダー要素とタイトル `<h1>` を生成して内部保持する。

---

==== setTitle(title)

.入力項目
[cols="2,2,4", options="header"]
|===
^.^|引数 ^.^|型 <.^|内容

<.^|`title` |string |更新後のタイトル文字列
|===

.出力項目
[cols="2,4", options="header"]
|===
^.^|型 <.^|内容

|this |`<h1>` テキストを更新してメソッドチェーンを返す
|===

---

==== attach()

[cols="2,2,4", options="header"]
|===
^.^|項目 ^.^|型 <.^|内容

.入力項目
なし

.出力項目
[cols="2,4", options="header"]
|===
^.^|型 <.^|内容

|this |`root.prepend` で DOM 先頭に挿入してメソッドチェーンを返す
|===

---

== 4. Footer クラス

=== 格納先

`src/ui/footer.js`

=== 説明

画面下部のフッター要素を生成し DOM へ追加する。

=== メソッド

==== constructor(root)

.入力項目
[cols="2,2,4", options="header"]
|===
^.^|引数 ^.^|型 <.^|内容

<.^|`root` |Element |フッターを挿入する親要素
|===

.出力項目
なし

NOTE: `<footer>` 要素にコピーライト文字列を設定して内部保持する。

---

==== attach()

[cols="2,2,4", options="header"]
|===
^.^|項目 ^.^|型 <.^|内容

.入力項目
なし

.出力項目
[cols="2,4", options="header"]
|===
^.^|型 <.^|内容

|this |`root.append` で DOM 末尾に挿入してメソッドチェーンを返す
|===

NOTE: コピーライト表記: `© 2026 THE GRAND MILLIONAIRE. All Rights Reserved.`
