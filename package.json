{
  "name": "quiz-game-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint \"quiz-app/src/**/*.js\"",
    "test": "node --test tests/**/*.test.js",
    "check": "npm run lint; npm run test"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
